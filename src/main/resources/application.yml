spring :
  datasource :
    url: 'jdbc:mysql://localhost:3306/house?autoReconnect=true&allowPublicKeyRetrieval'
    username : root
    password : 870412a!
    driver-class-name : org.mariadb.jdbc.Driver

  devtools:
    restart:
      enabled: false
  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      database : myboard
      username : admin
      #password :